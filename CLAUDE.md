# Working preferences

- Be token-efficient in all research and work on this repo: prefer targeted `Grep`/`Glob` lookups over broad exploration, read only the file sections needed instead of whole files, avoid re-reading files already seen in context, and keep tool calls and explanations minimal. Don't spawn subagents for tasks doable directly in 1-3 tool calls. Keep responses and commit messages concise.
